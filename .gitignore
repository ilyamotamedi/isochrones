node_modules
dist
dist-ssr

# Local env files — these hold the Mapbox token and must never be committed.
.env
.env.local
.env.*.local

.DS_Store
*.log
npm-debug.log*
.vite
coverage
.idea
.vscode/*
!.vscode/extensions.json

# Firebase CLI scratch: deploy caches, emulator state and debug logs. The
# config that matters — firebase.json, .firebaserc, firestore.rules — is
# committed on purpose.
.firebase/
firebase-debug.log*
firestore-debug.log*
.runtimeconfig.json
